/** Shared layout animation for group product reorder (siblings sliding into place). */
export const GROUP_PRODUCT_SORTABLE_TRANSITION = {
  duration: 220,
  easing: "cubic-bezier(0.25, 1, 0.5, 1)",
} as const
